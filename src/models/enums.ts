export enum Gender {
  Male = 'Male',
  Female = 'Female',
}

export enum EducationType {
  PreSchool = 'PreSchool',
  School = 'School',
  Course = 'Course'
}
export enum UserType {
  Teacher = "Teacher",
  Student = "Student",
  Employee = "Employee"
}

export enum UserStatus {
  Active = "Active",
  Passive = "Passive"
}